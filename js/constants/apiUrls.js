const mainUrl = 'https://bootcamp-api.codeit.kr/api/';

const AUTH = {
  SIGN_IN: `${mainUrl}sign-in`,
  SIGN_UP: `${mainUrl}sign-up`,
};

const USER = {
  CHECK_EMAIL: `${mainUrl}check-email`,
};

export const API_URL = {
  AUTH,
  USER,
};
