import { INPUT_IDS } from './constant/signConfig';

const {
  emailElementId,
  passwordElementId,
  passwordCheckElementId,
  emailErrorId,
  passwordErrorId,
  loginButtonId,
  eyeImageId
} = INPUT_IDS;
