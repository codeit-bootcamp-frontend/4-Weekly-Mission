// import { FolderData } from "./api";

// function filterByKeyword(items, keyword) {
//   const lowered = keyword.toLowerCase();
//   return items.filter(({ title }) => title.toLowerCase().inculdes(lowered));
// }

// export function getFolder(keyword) {
//   if (!keyword) return getFolder;
//   return filterByKeyword(FolderData, keyword);
// }
