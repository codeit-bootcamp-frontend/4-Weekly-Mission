export const ROUTE = {
  랜딩: "/",
  로그인: "/signin",
  개인정보처리방침: "/privacy",
  FAQ: "/faq",
};

const SECOND = 1000;
const MINUTE = SECOND * 60;
const HOUR = MINUTE * 60;
const DAY = HOUR * 24;
const MONT = DAY * 31;
const YEAR = MONT * 12;

export const TIME_IN_MILLISECONDS = {
  SECOND,
  MINUTE,
  HOUR,
  DAY,
  MONT,
  YEAR,
};
