import "../colors.css";
import "../global.css";
import { Layout } from "../components/Layout/Layout";

function FolderPage() {
  return (
    <div className="App">
      <Layout sticky={false}></Layout>
    </div>
  );
}

export default FolderPage;
