import styled from "styled-components";

export const Button = styled.button`
	border: none;
	background: none;
	font-size: 16px;
	color: #6d6afe;
	flex-shrink: 0;

	&:hover {
		cursor: pointer;
	}
`;
