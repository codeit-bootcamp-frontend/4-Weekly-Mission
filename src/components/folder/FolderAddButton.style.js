import styled from "styled-components"

export const AddFolder = styled.button`
  display: flex;
  align-items: center;
  font-weight: 500;
  color: var(--primary);

  img {
    width: 16px;
    height: 16px;
    margin-left: 4px;
  }
`
