import styled from "styled-components";
import CTA from "./CTA";

const ModalCTAred = styled(CTA)`
  width: 280px;
  padding: 16px 20px;
  font-size: 16px;
  background: var(--red);
`;

export default ModalCTAred;
