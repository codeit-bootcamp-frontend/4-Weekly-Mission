import styled from "styled-components";

export const CategoryNavButton = styled.button`
  display: flex;
  gap: 0.4rem;
  color: var(--gray60);
  font-size: 1.4rem;
  font-weight: 600;
`;

export const NavButtonIconContainer = styled.div`
  position: relative;
  width: 2rem;
  height: 2rem;
`;
